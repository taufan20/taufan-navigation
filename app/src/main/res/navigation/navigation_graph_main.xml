<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tool="http://schemas.android.com/tools"
    android:id="@+id/navigation_graph_main"
    app:startDestination="@id/listFragment">
    <fragment
        android:id="@+id/listFragment"
        android:name="com.taufan.navigation.ListFragment"
        android:label="ListFragment"
        tool:layout="@layout/fragment_list">
        <action
            android:id="@+id/action_listFragment_to_detailFragment"
            app:destination="@id/detailFragment" />
    </fragment>
    <fragment
        android:id="@+id/detailFragment"
        android:name="com.taufan.navigation.DetailFragment"
        android:label="DetailFragment"
        tool:layout="@layout/fragment_detail"/>
    <fragment
        android:id="@+id/newFragment"
        android:name="com.taufan.navigation.NewFragment"
        android:label="NewFragment"
        tool:layout="@layout/fragment_new"/>
</navigation>